<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <data>
    <template id="website_invoice_inherit_payment_online" inherit_id="account_payment.portal_invoice_page_inherit_payment">
      <xpath expr="//a" position="replace">
            <div>
                <a href="#" t-if="invoice.state == 'posted' and invoice.invoice_payment_state == 'not_paid' and invoice.amount_total and invoice.type == 'out_invoice' and (pending_manual_txs or not tx_ids or invoice.amount_residual) and invoice.pay_online" class="btn btn-primary btn-block mb-2" data-toggle="modal" data-target="#pay_with">
                    <i class="fa fa-fw fa-arrow-circle-right"/> Pagar ahora
                </a>
            </div>
        </xpath>
    </template>
  </data>
</odoo>
